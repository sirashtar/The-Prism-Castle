<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :job_name %>
  </div>

  <div class="field">
    <%= form.label :style %>
    <%= form.text_field :style, id: :job_style %>
  </div>

  <div class="field">
    <%= form.label :feauture %>
    <%= form.text_field :feauture, id: :job_feauture %>
  </div>

  <div class="field">
    <%= form.label :advantage %>
    <%= form.text_field :advantage, id: :job_advantage %>
  </div>

  <div class="field">
    <%= form.label :disadvantage %>
    <%= form.text_field :disadvantage, id: :job_disadvantage %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level, id: :job_level %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
